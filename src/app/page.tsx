import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Home | AMC",
};

const HomePage = () => {
  return (
    <div>
      <h1>Home!</h1>
    </div>
  );
};

export default HomePage;
